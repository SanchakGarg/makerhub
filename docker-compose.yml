services:
  makerhub:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
    volumes:
      - ./machines:/app/machines
      - ./data:/app/data
    restart: unless-stopped
