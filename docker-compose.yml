services:
  makerhub:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - BASE_URL=http://localhost:3000
    volumes:
      - ./machines:/app/machines
    restart: unless-stopped
